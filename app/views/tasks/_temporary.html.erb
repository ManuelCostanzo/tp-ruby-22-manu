<%= render partial: 'tasks/simple', locals: {t: t} %>

<div class="info">
	<span class="h5">Finaliza:</span>
	<strong><%= editable t, :ending, url: task_path(t), value: t.ending.strftime("%d/%m/%Y %k:%M") %></strong>	
</div>