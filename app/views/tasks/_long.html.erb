<%= render partial: 'tasks/simple', locals: {t: t} %>

<div class="info"
	<span class="h5">Porcentaje:</span>
	<strong><%= editable t, :percentage, url: task_path(t), value: "#{t.percentage.to_i}%" %></strong>	
</div>